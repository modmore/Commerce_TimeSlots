<div class="ui message centered">
    <h4>{{ confirm }}</h4>
    {{ message }}
</div>
<div class="ui hidden divider"></div>
<div class="ui horizontal divider">{{ lex('commerce_timeslots.note') }}</div>
<div class="ui centered">{{ note }}</div>
<div class="ui hidden divider"></div>
<div id="commerce-modal-actions" class="ui very basic vertical clearing segment">
    <button class="ui button commerce-modal-close" type="button" aria-label="{{ lex('commerce.close') }}">
        {{ lex('commerce.close') }}
    </button>
    <a href="{{ action_url }}" class="ui right labeled primary icon button green commerce-ajax-modal" aria-label="{{ lex('commerce_timeslots.populate') }}">
        {{ lex('commerce_timeslots.populate') }}
        <i class="icon icon-bolt"></i>
    </a>
</div>